# Metadata

`rh20t_task_descriptions.json` is copied from the official RH20T task metadata:

<https://rh20t.github.io/static/task_description.json>

The training loader uses `task_description_english` as the DistilBERT input.
